# Reference for shell/container tooling; dotnet run does not auto-load this file.
# The current baseline needs no secrets or API keys.
ASPNETCORE_URLS=http://localhost:5080
ASPNETCORE_ENVIRONMENT=Development
